import { ArrowUpRight,Check } from 'lucide-react';import { motion } from 'framer-motion';import { SectionHeading } from './SectionHeading'
const prices=[{tag:'Самая частая',title:'Экскаватор-погрузчик',price:'3 200',unit:'₽ / час',list:['С экипажем','Минимум 4 часа','Подача от 90 минут']},{tag:'Логистика',title:'Самосвал 20 м³',price:'2 900',unit:'₽ / час',list:['До 25 тонн','Контроль маршрута','Закрывающие документы']},{tag:'Подъём',title:'Автокран 25 т',price:'4 600',unit:'₽ / час',list:['Вылет стрелы 28 м','Аттестованный экипаж','ППР по запросу']}]
export function PricingSection(){return <section id="prices" className="relative z-10 border-y border-line bg-panel py-24 lg:py-36"><div className="container-pad"><SectionHeading index="04" label="ориентиры" title="Понятная экономика до старта" copy="Финальная стоимость зависит от адреса, смены и условий объекта. Смету фиксируем до выезда."/><div className="grid lg:grid-cols-3">{prices.map((p,i)=><motion.article initial={{opacity:0,y:30}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.1}} key={p.title} className={`group border border-line p-7 ${i===1?'relative bg-orange text-white lg:-translate-y-4 lg:shadow-glow':'bg-ink'}`}><div className="mb-12 flex items-center justify-between text-[9px] font-bold uppercase tracking-[.2em]"><span className={i===1?'text-white/70':'text-orange'}>{p.tag}</span><span className="text-white/25">0{i+1}</span></div><h3 className="min-h-14 font-display text-xl uppercase">{p.title}</h3><div className="my-7"><strong className="font-display text-4xl">{p.price}</strong><span className="ml-2 text-xs text-white/45">{p.unit}</span></div><ul className="space-y-3 border-t border-white/15 pt-6">{p.list.map(x=><li key={x} className="flex gap-3 text-xs text-white/60"><Check className="size-4"/>{x}</li>)}</ul><a href="#contact" className="mt-8 flex items-center justify-between border-t border-white/15 pt-5 text-[10px] font-bold uppercase tracking-wider">Рассчитать задачу <ArrowUpRight className="size-4 transition group-hover:rotate-45"/></a></motion.article>)}</div></div></section>}
